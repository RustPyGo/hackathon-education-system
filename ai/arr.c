#include<stdio.h>
#include<math.h>

void reversArr(int arr[], int size); //đảo ngược mảng

int main(){
    int arr[100] = {0};
    int arr2[100] = {0};
    int size = 0, size2 = 0;
    int devidedSize = 0;
    inputArr(arr, &size);
    // outputArr(arr, size);
    // cpyArr(arr, size, arr2, &size2);
    // inputArr(arr2, &size2);
    // concatArr(arr, &size, arr2, size2);
    // outputArr(arr, size);
    // outputArr(arr, size);
    // devideArr(arr, &size, 3, arr2, &size2);
    // outputArr(arr2, size2);
    // printf(" '%d' ", mostApearElement(arr, size));
    // reversArr(arr, size);
    // outputArr(arr, size);
    // delAllKey(arr, &size, 3);
    outputArr(arr, size);
    
    return 0;
}

void reversArr(int arr[], int size) {
    for ()
}